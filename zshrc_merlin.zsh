# zshrc local de merlin

# PATH
export PATH="$HOME/.local/bin:$PATH"

# alias
alias ls="ls -la --color=auto"
alias radio="python3 $HOME/radio-select/radio-select.py"

# message
smsg -b "SERVEUR DE PRODUCTION"